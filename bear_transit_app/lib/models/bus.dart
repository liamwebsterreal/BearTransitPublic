import 'package:flutter/material.dart';
class Bus {
  String name;
  String hours;
  String interval;
  double rate;
  AssetImage profilePic;
  String info;

  //String capacity;

  Bus(this.name, this.hours, this.interval, this.rate, this.profilePic, this.info);

  get status => null;
}